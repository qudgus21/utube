extends layouts/main

block content
    .form_container
        form(action=routes.login, method="post",required=true)
            input(type="email",placeholder="email",name="email",required=true)
            input(type="password",placeholder="verify password",name="password",required=true)
            input(type="submit",value="log Now")
        include partials/socialLogin